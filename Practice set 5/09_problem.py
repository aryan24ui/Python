#Can you change the values inside a list which is contained in set S?

s = {8, 7, 12, "Harry", [1,2]}
#Not possible
'''Set elements must be immutable (hashable).

A list is mutable, so it cannot be a set element.

'''
